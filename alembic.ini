 [alembic]
# path to migration scripts
script_location = alembic

# template used to generate migration file names; The default value is %%(rev)s_%%(slug)s
# Uncomment the line below if you want the files to be named with just the revision number and migration name like `` instead
# file_template = %%(rev)s_%%(slug)s

# sys.path path separator as used in string.path.join also
# the value separator in the alembic.ini-style substitution as used in migration files
# and environment.py file.
# the character used by the filesystem as the path separator
# (i.e. should be / on Unix systems and on Windows)
# sqlalchemy.url driver://user:password@localhost/dbname
# For PostgreSQL with asyncpg: postgresql+asyncpg://user:password@host/dbname
# For synchronous SQL (Alembic): postgresql://user:password@host/dbname
# This will be overridden by env.py which reads from config.settings
sqlalchemy.url = postgresql://postgres:Supposedbe5@database-1.cf2w2gwcmvc8.eu-north-1.rds.amazonaws.com:5432/Finanza

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console
qualname =

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
